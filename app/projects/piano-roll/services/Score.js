// @flow
export default class Score {
  constructor(context: CanvasRenderingContext2D, width: number, height: number) {
    this.ctx = context;
    this.width = width;
    this.height = height;
  }

  draw() {}
}
